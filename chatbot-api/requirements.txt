# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# LLM
anthropic

# Milvus vector DB
pymilvus==2.3.7
marshmallow==3.23.2

# Embedding — must match Vectorize_To_Milvus.py (all-MiniLM-L6-v2)
sentence-transformers==3.3.1
torch==2.5.1
torchvision==0.20.1

# Hybrid search — BM25 sparse retriever
rank-bm25==0.2.2
numpy>=1.26.0

# Trino SQL engine client
trino==0.329.0

# CV matching — PDF extraction
pdfplumber==0.11.4
python-multipart==0.0.20

# OCR fallback for scanned / image-only PDFs
PyMuPDF>=1.24.0
paddlepaddle==3.0.0
paddleocr==3.5.0

# MinIO object storage client
minio==7.2.15

# Config & validation
pydantic==2.10.3
pydantic-settings==2.7.0
python-dotenv==1.0.1
